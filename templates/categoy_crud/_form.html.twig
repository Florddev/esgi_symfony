{{ form_start(form, {'attr': {'class': 'mt-8 space-y-6'}}) }}
<div class="space-y-4">
    {{ form_row(form.nom) }}
    {{ form_row(form.label) }}
</div>

<button class="mt-4 w-full bg-red-600 text-white font-bold py-2 px-4 rounded-lg hover:bg-red-700">
    {{ button_label|default('Enregistrer') }}
</button>
{{ form_end(form) }}